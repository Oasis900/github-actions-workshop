name: Try reading a secret

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Time to read the secret
        shell: bash
        run: |
          echo "The secret variable name is : ITS_A_SECRET "
          echo "The contents is ${{ secrets.ITS_A_SECRET }}"